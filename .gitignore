# Virtual environments
venv/
env/
.venv/

# Cache / compiled artifacts
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/

# Test / lint / type-check caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment
.env
.env.*
!.env.example

# Local databases & generated data
# Per-meeting KuzuDB directories are auto-created on first run.
database/

# Defensive ignore for any audio scratch files placed in the project root.
# Note: the pipeline uses tempfile.mkstemp() which writes to the OS temp
# directory, so these patterns are precautionary only.
speaknode_*.mp3
speaknode_*.wav
speaknode_*.m4a
speaknode_*.ogg
temp_*.mp3
temp_*.wav
temp_*.m4a
temp_*.ogg

# Shared cards output
shared_cards/

# Logs & temp files 
*.log
*.tmp
*.bak

# Editor / OS noise
.DS_Store
Thumbs.db
.idea/
.vscode/settings.json
*.swp
*~

# AI agent artifacts 
.gemini/